Migrate new homepage to root and move blog to /blog

- Moved existing Jekyll blog content (`index.markdown`, `about.markdown`, `designs/`) to `blog/`.
- Updated `_config.yml` to set permalinks for posts and designs to start with `/blog`.
- Updated links in `blog/index.markdown` and `blog/about.markdown`.
- Copied new homepage (`index.html`), `css/`, `assets/`, and legacy `home/` pages to root.
- Updated `index.html` to link to the Blog at `/blog/`.
- Renamed `_images` to `assets/images` in the source to comply with Jekyll static file handling.

Gemini commit: Merge new homepage with existing Jekyll blog.